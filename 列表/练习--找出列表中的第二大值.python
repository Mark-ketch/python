#找出列表中的第二大的值
listNum=[]
num1=0
while num1<10:
    val=int(input())
    listNum.append(val)
    num1+=1
print(listNum)
#升序排序
listNum.sort()
print(listNum)

count=listNum.count(listNum[len(listNum)-1])
c=0
while c < count:
    listNum.pop()
    c+=1
print(listNum[len(listNum)-1])
